{
  "name": "railway-visit-app",
  "version": "1.0.0",
  "description": "Express + Puppeteer app that runs a batch of 50 page visits with rotating IPs, random referral and device.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "express": "^4.18.2",
    "puppeteer": "^21.0.0"
  },
  "engines": {
    "node": ">=14"
  }
}
